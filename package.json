{
  "name": "internship-management",
  "version": "1.0.0",
  "private": true,
  "description": "Internship Management System - Monorepo",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "type-check": "turbo run type-check",
    "install:all": "pnpm install",
    "setup": "pnpm install && pnpm build"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.0",
    "eslint": "^8.45.0",
    "prettier": "^3.0.0",
    "turbo": "^1.10.0",
    "typescript": "^5.1.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "npm@10.0.0",
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "axios": "^1.12.2"
  }
}
