package main

import (
	"fmt"
	"log"
)

func main() {
	fmt.Println("🌱 Database seeding...")
	log.Println("Seed command is currently disabled - using Prisma ORM which is not compatible with current GORM setup")
	fmt.Println("✅ Seed command completed (no-op)")
}