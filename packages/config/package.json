{
  "name": "@internship/config",
  "version": "1.0.0",
  "description": "Shared configurations",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.1.0"
  },
  "files": [
    "dist",
    "tailwind.config.js",
    "postcss.config.js"
  ]
}